import React from "react";

const ChatBot = () => (
  <div>
    <h2 className="text-2xl font-semibold mb-4">Chatbot</h2>
    <p>Interact with the chatbot to assist with queries.</p>
  </div>
);

export default ChatBot;
